//! Participant-domain types, re-exported from `crate::types` for a focused,
//! reviewable surface without changing the original type paths.

pub use crate::types::{
    CertificationLevel,
    LocationRecord,
    ParticipantRole,
    ParticipantTier,
    PermissionAuditEntry,
    PermissionType,
    QualityScore,
    ReputationBadge,
};
